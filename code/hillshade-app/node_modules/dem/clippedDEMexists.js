const fs = require('fs');

module.exports = function clippedDEMexists(_params) {
  var path = './dem/gen/state/colorado/' + _params.area + '/' + _params.area + '.tif';
  return fs.existsSync(path);
}
